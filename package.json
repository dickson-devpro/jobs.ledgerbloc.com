{
  "name": "finance-static-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "check": "node scripts/check-similarity.mjs",
    "generate": "node scripts/generate-job-article.mjs",
    "expand": "node scripts/expand-matrix.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "markdown-it": "^14.3.0",
    "markdown-it-anchor": "^9.2.1",
    "turndown": "^7.2.4"
  }
}